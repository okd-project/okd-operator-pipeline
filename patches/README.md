# Operator Patches

Here you can find patches for all components used to build the OKD variants of the OpenShift operators.
The directory structure is as follows:

```
patches/
├── <recipe name>/
│   ├── <component name>/
│   │   ├── <version>/
│   │   │   ├── <patch files>
```