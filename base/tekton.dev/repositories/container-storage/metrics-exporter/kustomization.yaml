generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ocs-metrics-exporter-patch
    namespace: okd-team
    files:
      - 0001-OKDify.patch

resources:
  - repository.yaml
  - pvc.yaml